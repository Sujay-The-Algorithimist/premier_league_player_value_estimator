import type { MetaData, MetricsData } from "../types";

export function Method({ meta, metrics }: { meta: MetaData; metrics: MetricsData }) {
  return <section className="method-page" aria-labelledby="method-title"><p className="kicker">Method & limitations</p><h1 id="method-title">The model is only as honest as its timestamp.</h1><p className="analysis-lede">This project estimates a player’s market value from profile and recent performance. It uses a time-based test split so the model never trains on a future season.</p><div className="method-grid"><article><span>01 / Source</span><h2>Published historical snapshot</h2><p>{meta.source}. The interface was generated {meta.generated_at}; it is a historical analysis, not live market intelligence.</p></article><article><span>02 / Target</span><h2>Log market value</h2><p>Values are modelled on log(1 + EUR value), which reduces the influence of a small number of exceptionally expensive players.</p></article><article><span>03 / Split</span><h2>{metrics.split.train} → {metrics.split.test}</h2><p>All reported model metrics use the later seasons as holdout data. Results are not in-sample scores.</p></article><article><span>04 / Guardrail</span><h2>Season first, valuation second</h2><p>Season statistics must be available before the attached valuation date. Rows outside the post-season window are excluded from supervised training.</p></article></div><article className="limitations"><p className="kicker">Important limitation</p><h2>Useful signal, incomplete picture.</h2><p>Injuries, contract length, negotiations, agent influence, commercial profile, transfer clauses, and club strategy are not fully observed. Treat every output as an analytical estimate—not a fair price or a recommendation to buy or sell.</p></article></section>;
}
